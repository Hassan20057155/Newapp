workflows:
  default-workflow:
    name: Build APK for Rewan
    environment:
      groups:
        - keystore
    scripts:
      - name: Build APK
        script: |
          flutter pub get
          flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
